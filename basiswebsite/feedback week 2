1x h1 gebruiken
meerdere keren ul gebruiken in nav1
class op sections (geen id)
end of type (nth)

todo:
clean code breakdown

cursor = X tenzij button
img eigen container (flex-wrap + flexbase)
- width 

padding op article
